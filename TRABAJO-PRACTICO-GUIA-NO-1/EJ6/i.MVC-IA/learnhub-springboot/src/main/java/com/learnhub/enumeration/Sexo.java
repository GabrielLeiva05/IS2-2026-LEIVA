package com.learnhub.enumeration;

/**
 * Valores controlados para el dato sexo solicitado en el registro del docente.
 * Al ser enum se evita guardar texto libre con valores inconsistentes.
 */
public enum Sexo {
    MASCULINO,
    FEMENINO,
    OTRO,
    PREFIERO_NO_DECIR
}
